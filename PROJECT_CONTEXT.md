# 📚 مستند سياق المشروع (OED Training Management System)
هذا الملف هو العقل المدبر والمرجع الأساسي لمشروع "OED Training Management System".
تم إنشاؤه لكي يساعد أي مهندس أو نظام ذكاء اصطناعي (AI) على فهم المشروع بالكامل في ثانية واحدة واستكمال العمل عليه بدون فقدان أي تفاصيل.

---

## 1. 🏗️ نظرة عامة على التطبيق (Overview)
- **وصف التطبيق:** تطبيق ويب لشركة أوراسكوم لإدارة تدريبات الموظفين، يسمح للمتدربين بطلب الانضمام، وللمشرفين والمديرين بإدارة الجلسات التدريبية ومتابعة الحضور والتقييم.
- **لغة البرمجة:** React (TypeScript) + Vite
- **التصميم:** Tailwind CSS (بألوان شركة أوراسكوم: الكحلي `#002D62` والأصفر `#FFC000`).
- **الاستضافة:** Vercel (مربوط مباشرة بـ GitHub).
- **قاعدة البيانات:** Firebase Cloud Firestore (تم دمجها بنجاح للعمل في الوقت الفعلي Real-time).

---

## 2. 🗄️ هيكلة قاعدة البيانات (Database Structure - Firebase)
التطبيق يستخدم Firebase (مجموعة `db` في `src/firebase.ts`) ويتكون من المجموعات التالية (Collections):

1. **`users`**:
   - تحتوي على بيانات المستخدمين: `(id, hrCode, name, phone, department, jobRole, role, status, password)`.
   - الصلاحيات (`role`): `trainee` (متدرب)، `manager` (مدير)، `admin` (مشرف).
   - الحالة (`status`): `pending` (قيد الانتظار)، `approved` (مقبول)، `rejected` (مرفوض).

2. **`sessions`**:
   - تحتوي على الجلسات التدريبية: `(id, courseName, instructor, date, duration, maxAttendees, location, registeredUsers, unregisteredUsers, status)`.

3. **`cleanedData`**:
   - تحتوي على السجلات التاريخية للتدريبات المرفوعة عبر ملفات الإكسيل (Analytics).

*ملاحظة: التطبيق يستخدم `onSnapshot` لجلب البيانات وتحديثها فورياً على كل الأجهزة.*

---

## 3. 🔐 نظام تسجيل الدخول والصلاحيات (Authentication & Roles)
- **تسجيل الدخول:** مبرمج يدوياً (Custom Login) داخل `src/components/Login.tsx` يعتمد على `hrCode` كاسم مستخدم و `password`.
- **ملاحظة هامة جداً:** يوجد حساب "مشرف سري" مدمج في الكود للحالات الطارئة:
  - اسم المستخدم: `admin`
  - الباسورد: `admin123`
- بمجرد الدخول، يوجه التطبيق `App.tsx` المستخدم بناءً على صلاحياته إلى لوحة التحكم المناسبة: `TraineeDashboard`, `ManagerDashboard`, أو `AdminDashboard`.

---

## 4. 🔄 نظام التحديث التلقائي (PWA Auto-Update)
- التطبيق يعمل كـ **Progressive Web App (PWA)**، مما يعني أنه ينزل على هواتف الموظفين ويعمل كبرنامج أصلي.
- تم استخدام مكتبة `vite-plugin-pwa` مع خاصية `registerType: 'autoUpdate'`.
- **كيف يعمل؟** عندما تقوم برفع كود جديد لـ Vercel، سيكتشف هاتف الموظف التحديث في الخلفية بصمت، ويقوم بتحميله بدون أي رسائل مزعجة. وعند إعادة فتح التطبيق، سيجد النسخة الجديدة.

---

## 5. 🚀 دليل النشر والتعديل (Deployment Guide)
أهم شيء يجب تذكره هو أن Vercel ينظر إلى الكود الموجود في **الصفحة الرئيسية للمشروع (Root Directory `./`)**.
عندما تقوم بعمل أي تعديل على هذا اللابتوب، اتبع الخطوات التالية لرفعه للجميع:

1. افتح صفحة مشروعك على موقع **GitHub** (الصفحة الرئيسية).
2. اضغط **Add file** ثم **Upload files**.
3. اسحب الملفات أو المجلدات التي تم تعديلها (مثلاً: `src`، `package.json`، الخ) وضعها هناك.
4. انقر على **Commit changes**.
5. Vercel سيبدأ البناء أوتوماتيكياً. انتظر دقيقة واحدة، وسيصل التحديث لكل الموظفين بهدوء!

---

## 6. 🛣️ خطة العمل المستقبلية (Roadmap / Next Steps)
أشياء طلبها مدير المشروع (User) ويمكن استكمالها في المحادثات القادمة:
- [ ] **سبب الرفض:** تفعيل ميزة إرسال سبب الرفض للموظف عندما يقوم المشرف برفض طلبه.
- [ ] **تحسين لوحة المدير:** إضافة ميزات تفاعلية أكثر في `ManagerDashboard`.
- [ ] **أمان إضافي (اختياري):** الانتقال من الـ Custom Login إلى Firebase Authentication لو أردنا إغلاق القاعدة بـ Production Mode لاحقاً.

---
**تنويه للذكاء الاصطناعي (AI Prompt Context):**
إذا كنت تقرأ هذا الملف في بداية محادثة جديدة، فهذا يعني أن العميل يطلب منك استكمال تطوير هذا المشروع. ابدأ بقراءة هذا الملف لتعرف السياق، ثم اسأله عن الميزة التي يريد تنفيذها من الـ Roadmap!
